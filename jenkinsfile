pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Building the project....'
                echo "DEBUG: parameter foo = ${Test}"
            }
        }
        stage('deploy') {
            steps {
                echo 'Deploying the project...'
            }
        }
        stage('test') {
            steps {
                echo 'tests passed'
            }
        }
    }
}
